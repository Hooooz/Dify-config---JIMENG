{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn app:app --app-dir jimeng-dify-service --host 0.0.0.0 --port $PORT"
  }
}
