{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn app:app --app-dir jimeng-dify-service --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
